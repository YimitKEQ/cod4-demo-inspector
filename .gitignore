# Game assets and demos never go in the repo
demos/*
!demos/README.md
# Extracted map assets are published deliberately, so the live site shows the
# real map rather than the reconstruction. See maps3d/NOTICE.md.
# The intermediate dumps they are built from stay out.
maps3d/**/*.dds
maps3d/**/*.iwi
renders/
game/

# Tooling
node_modules/
__pycache__/
*.pyc
.venv/
dist/
*.log
.DS_Store
Thumbs.db

# Local config with machine paths
tools/config.local.json
.claude/settings.local.json

# The personal brief this was built from stays local
HANDOFF.md
